participant("FirstLetterOfBookName")

description("First letter of book name")

property(
    name="type"
    description="Data type of participant (list, object, str etc)"
    type="string"
    get="getType"
)

domain(
    type="string",
    length=1
)

dcs (
    # Inherent domain
    # Domain structure for compression
)