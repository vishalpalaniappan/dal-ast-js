design ("lexer")

behavior sample(a,b) {
    while (isValid) {
        design ("reusing commands I support")
    }
}

behavior sample2(test) {
    while (notDone) {
        design ("reusing commands I support")
    }
}