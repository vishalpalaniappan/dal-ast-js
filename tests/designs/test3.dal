design ("lexer")

behavior sample {
    getInput(name)
    set(isValid,["test"],true)
    while (isValid) {
        set(sampleVar,[],"this is a string")
    }
}

behavior sample2 {
    set(notDone,[],false)
    while (notDone) {
        set(sampleVar2,[],["this is a list"])
    }
}