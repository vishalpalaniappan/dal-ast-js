design ("lexer")

behavior sample(a,b) {
    getInput(name)
    set(isValid,[],true)
    while (isValid) {
        set(sampleVar,[],"this is a string")
    }
}

behavior sample2(test) {
    set(notDone,[],false)
    while (notDone) {
        set(sampleVar2,[],["this is a list"])
    }
}