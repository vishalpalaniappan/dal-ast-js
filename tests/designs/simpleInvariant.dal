design ("lexer")

behavior getName {
    getInput(name, "Provide Name")
    invariant {
        # Prevent reaching getFirstLetterOfName behavior
        # if invariant is violated
        prevent(getFirstLetterOfName)
        getLength(nameLength, name)
        isGreaterThan(isValid, nameLength, 0)
        if (isValid) {
            assert(true)
        }
    }
    set(worldState, ["name"], name)
    select(getFirstLetterOfName)
}

behavior getFirstLetterOfName {
    get(name, worldState, ["name"])
    getFromPos(firstLetter, name, 0)
    set(worldState, ["firstLetter"], firstLetter)
    select(getName)
}

