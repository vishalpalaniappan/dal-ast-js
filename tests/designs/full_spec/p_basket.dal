participant("basket")

property(
    name="length"
    description="Number of books in basket"
    type="number"
)

property(
    name="type"
    description="Type of object (list, object, str etc)"
    type="string"
)

domain(
    contains="books"
)

dcs(
    # Domain structure for compression
)

getLength()

getType()