design ("lexer")

behavior sample("test") {
    design ("lexer")

    if (isEqual) {
        add (1,2)
    } else {
        subtract(1,2)
    }

    for (counter, 0, 10) {
        square (counter)
    }

    while (condition) {
        customCommand ("arg1", "arg2")
    }
}